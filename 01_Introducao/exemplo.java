class Exemplo {
  public static void main(String[] args) {
    // Condicionais
    // --------- IF/ELSE --------- 
    if (condicao) {
      // executar código caso `condicao` seja true
    } else if (condicao2) {
      // executar código caso `condicao` seja false
      // mas `condicao2` seja true
    } else {
      // executar código caso todos os blocos anteriores sejam false
    }
    // --------- SWITCH/CASE --------- 
    switch (chave) {
      case valor:
        // Executar caso chave == valor
        break;
      case valor2:
        // Executar caso chave == valor2
        break;
      default:
        // Executar caso chave não seja igual a nenhum valor anterior
        break;
    }
    // --------- TERNÁRIO --------- 
    condicao 
      ? // executar caso condicao == true
      : // executar caso condicao == false
    ;
    // =============================================

    // Operadores Lógicos
    if (3 == 5) 
  }
}


